spec_version: '0.1'
operator:
  id: nexus.video.ltx23.submit-render
  version: 0.1.0
  display_name: "Submit LTX-2.3 Render"
  description: "Submit a planned render to the runtime; returns the run_id."
inputs:
  - name: plan
    type: object
    required: true
outputs:
  - name: run_id
    type: string
  - name: status
    type: string
