spec_version: '0.1'
operator:
  id: nexus.video.ltx23.plan-long-video
  version: 0.1.0
  display_name: "Plan LTX-2.3 Long Video"
  description: "Validate a CreateRenderRequest and compute a RenderPlan (no GPU work)."
inputs:
  - name: request
    type: object
    required: true
outputs:
  - name: plan
    type: object
