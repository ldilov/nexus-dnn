# Spec 032 — test-echo-runtime-2 worker (US4 fixture). Stdlib only.

[project]
name = "echo-worker-v2"
version = "0.0.1"
description = "Second test echo worker — prefix variant used by US4 multi-extension tests"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/echo_worker_v2"]
