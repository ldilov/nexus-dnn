# Build/test caches — never commit
**/__pycache__/
**/.pytest_cache/
**/.ruff_cache/
**/.venv/

# web deps (committed web/dist is the local-dev + fallback copy, kept)
web/node_modules/

# uv build artifacts
worker/.uv/
worker/uv-cache/

# Vendored Arc2Avatar method code is committed (GPU spike, 2026-06-30); only its
# run artifacts stay out of git (subjects/models are populated at runtime).
worker/src/arc2avatar/subjects/
worker/src/arc2avatar/models/
