# nexus-models-store

Host-owned model store — install pipeline, blob dedup, lease budget, quantization matching, resolver, verify, layout, reclaim.

Extracted from `nexus-backend-runtimes` as part of spec 018 (Deployments). `nexus-backend-runtimes` no longer owns the models store after the split.

Phase 1 skeleton only. Full code move lands in Phase 7 (US5 — Crate split).
