# P0.5 — Frozen contracts (trellis2), from P0-observed facts

> The gate made P0 the contract authority. These contracts are frozen from what actually ran on the
> GB10 (see [P0-COMPLETE](2026-06-24-trellis2-P0-COMPLETE.md)). P1–P4 build against THESE; do not
> re-guess. Scope = MVP-0 (image→MeshOnly→GLB; texture/multiview/viewer = P1).

## Extension identity
- id `nexus.3d.trellis2`, publisher `nexus`, storage alias `trellis2` (tables `ext_trellis2__*`).
- Runtime family native; worker = python/stdio; profiles `fake` + `gb10-flash` (cuda13-aarch64).

## Operator — `trellis2.generate_3d@1.0.0`
Inputs: `image` (artifact ref, required, RGBA/alpha = mask) · `seed` (int) · `sparse_steps` (default 12)
· `shape_steps` · `simplify_target` (default 1_000_000) · `texture` (bool, **default false** for MVP-0)
· `residency` (low_vram|balanced, profile-gated).
Outputs: `mesh_glb` (artifact, mime `model/gltf-binary`) · `metadata_json`.
(Text→3D, cascade, multiview = later versions; keep the input seam.)

## Worker RPC (stdio JSON-RPC 2.0, mirror svi2-pro)
- `trellis2.runtime.health` (handshake; loads model, returns ready) · `trellis2.generate.start`
  (streams `trellis2.generate.progress` notifications: stage, step/total) · `trellis2.generate.cancel`
  · `runtime.release_memory`. Error codes: GPU_NOT_SUPPORTED -32100, MODEL_MISSING -32101,
  GENERATION_FAILED -32102, CANCELLED -32103.

## Artifact + metadata
- GLB written by `o_voxel.postprocess.to_glb` → host artifact store (`store(bytes, name, "model/gltf-binary")`),
  served via extension `/api/v1/extensions/nexus.3d.trellis2/media/:ref` (range + Content-Disposition).
- metadata_json: `{ attention_backend:"flash_attn", compute_cap:"12.1", native_sm121:true (no spoof),
  stage_timings:{load_s, run_s}, mesh:{vertices,faces}, fallbacks:[], profile:"gb10-flash",
  models:{trellis:"microsoft/TRELLIS.2-4B", dinov3:<id>} }`.

## Runtime-health (per-dep) schema
Per native dep record `{ name, import_smoke:bool, kernel_smoke:bool, wheel_provenance, fallback_used }`
for cumesh / flex_gemm / o_voxel / nvdiffrast / flash_attn. A failed dep ⇒ profile `NotSatisfied`, not a crash.

## Proven runtime profile (gb10-flash)
torch 2.12.0+cu132 · py3.12 · ATTN_BACKEND=flash_attn · transformers==4.56.0 · 4 vendored kernels
(`binaries/linux-aarch64/`) + vendored flash_attn-2.8.3 · `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`.
dinov3 gated (official `facebook/...`; operator HF token must hold the grant; non-gated mirror
`kiennt120/...` is the dev fallback). RMBG skipped (`rembg_model=None`, `preprocess_image=False`).

## Fan-out (build against the above)
- **P1 rust shim** `trellis2-extension` crate (router/dispatcher/lease/storage/media route) + nexus-builtins wiring.
- **P2 worker** `trellis2_worker` (RPC + pipeline applying the P0-COMPLETE recipe + fake backend).
- **P3 web** extension bundle (recipe/DAG views, progress/Stop, download; 3D `<model-viewer>` = P1-later).
- **P4 model-artifact + install** (manifest decls + uv.lock with vendored wheels + aarch64 Dockerfile path).
