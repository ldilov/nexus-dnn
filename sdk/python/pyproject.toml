[project]
name = "nexus-sdk"
version = "0.1.0"
description = "Python SDK for nexus-dnn extension workers"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
