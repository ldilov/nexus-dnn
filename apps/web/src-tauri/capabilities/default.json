{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for nexus-dnn-desktop. Spec 042 phase-2 task T025 expands this with the full window + tray + IPC command surface.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
